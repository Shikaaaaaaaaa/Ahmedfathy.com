(()=>{const archiveCss=document.createElement('link');archiveCss.rel='stylesheet';archiveCss.href='assets/css/archive.css';document.head.append(archiveCss);const root=document.body,theme=document.querySelector('#theme');const saved=localStorage.getItem('portfolio-theme');if(saved==='dark')root.classList.add('dark');theme.addEventListener('click',()=>{root.classList.toggle('dark');localStorage.setItem('portfolio-theme',root.classList.contains('dark')?'dark':'light');document.querySelector('meta[name="theme-color"]').content=root.classList.contains('dark')?'#0c0f14':'#f3f2ed'});const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.1});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));document.querySelectorAll('a[href^="#"]').forEach(link=>link.addEventListener('click',event=>{const target=document.querySelector(link.getAttribute('href'));if(target){event.preventDefault();target.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'})}}));const filters=[...document.querySelectorAll('.archive-filters button')],rows=[...document.querySelectorAll('.archive-row')];filters.forEach(button=>button.addEventListener('click',()=>{filters.forEach(item=>item.classList.toggle('active',item===button));rows.forEach(row=>row.classList.toggle('hidden',button.dataset.filter!=='all'&&row.dataset.category!==button.dataset.filter))}))})();
